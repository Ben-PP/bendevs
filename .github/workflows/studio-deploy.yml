# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy studio to Firebase on merge
'on':
  push:
    branches:
      - main
    paths:
      - 'packages/studio/**'
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: yarn --immutable
      - run: yarn workspace studio build
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_BENDEVS_81FE2 }}'
          channelId: live
          projectId: bendevs-81fe2
          entryPoint: ./packages/studio
